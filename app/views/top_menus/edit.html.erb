<h2><%= link_to l(:label_top_menu_plural), top_menus_path %> &#187; <%= h(@top_menu) %></h2>
<% labelled_form_for (@top_menu) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
    <div class="btn-footer">  <%= submit_tag l(:button_update),:class=>'button-blu' %> </div>
<% end %>
