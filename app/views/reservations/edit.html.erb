<h2><%= link_to l(:label_reservation_plural), reservations_path %> &#187; <%= h(@reservation) %></h2>
<% labelled_form_for (@reservation) do |f| %>
  <%= submit_tag l(:button_save) %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_update) %>
<% end %>
